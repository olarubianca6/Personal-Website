[build]
  command = "npm run build"
  publish = "build"

[dev]
  command = "npm start"
  targetPort = 3000
  framework = "create-react-app"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200